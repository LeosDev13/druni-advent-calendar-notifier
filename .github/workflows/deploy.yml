name: Deploy to DigitalOcean Droplet

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker image
        run: |
          docker build -t my-app:latest .

      - name: Save Docker image to tar file
        run: |
          docker save my-app:latest -o my-app.tar

      - name: Deploy Docker image on Droplet
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.DROPLET_IP }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_PRIVATE_KEY }}
          port: 22
          script: |
            ls -la

            scp -o StrictHostKeyChecking=no my-app.tar ${{ secrets.DROPLET_USER }}@${{ secrets.DROPLET_IP }}:/root/my-app.tar

            echo "TOKEN=${{ secrets.TOKEN }}" > /root/my-app/.env
            echo "CHAT_ID=${{ secrets.CHAT_ID }}" >> /root/my-app/.env

            docker load -i /root/my-app.tar

            docker stop my-app || true
            docker rm my-app || true
